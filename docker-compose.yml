services:
  agent-control-room:
    build: .
    container_name: agent-control-room
    ports:
      - "${MC_PORT:-3000}:3000"
    env_file:
      - path: .env
        required: false
    volumes:
      - mc-data:/app/.data
    restart: unless-stopped

volumes:
  mc-data:
